# FreeVi - Dependencies
# =====================
# Install with: pip install -r requirements.txt
# Requires Python >= 3.10

# PDF reading
PyMuPDF>=1.24.0

# Local LLM via Ollama
ollama>=0.4.0

# TTS - Kokoro 1.0 with ONNX Runtime
kokoro-onnx>=0.4.4
soundfile>=0.12.0

# G2P for phonemization (espeak-ng backend)
# misaki 0.7.x supports Python 3.13
misaki>=0.7.0

# AI Word-level subtitle alignment (for Pro sync method)
faster-whisper>=1.0.0

# Environment variable loading (.env file support)
python-dotenv>=1.0.0

# Stock video download
requests>=2.31.0

# Video editing and assembly (MoviePy 2.x)
moviepy>=2.0.0

# AI Slide Generation
Pillow>=10.0.0
cairosvg>=2.5.0

# GUI (optional — only needed to run freevi_gui.py)
PyQt6>=6.6.0

# SYSTEM dependencies (not installed via pip):
# - ffmpeg: required by moviepy to encode video
#     Ubuntu/Debian: sudo apt install ffmpeg
#     macOS:         brew install ffmpeg
#     Arch:          sudo pacman -S ffmpeg
# - espeak-ng: required by misaki for phonemization
#     Ubuntu/Debian: sudo apt install espeak-ng
#     macOS:         brew install espeak-ng
#     Arch:          sudo pacman -S espeak-ng
# - cairo (required by cairosvg):
#     Ubuntu/Debian: sudo apt install libcairo2 libcairo2-dev
#     macOS:         brew install cairo
#     Arch:          sudo pacman -S cairo

# TTS - VibeVoice (Optional, requires Git and flash-attn)
# pip install git+https://github.com/microsoft/VibeVoice.git
# pip install flash-attn --no-build-isolation
